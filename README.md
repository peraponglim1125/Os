# Face Attendance Project

## Project Structure

```
face-attendance/
|
|-- docker-compose.yml          # ใช้รันบนเครื่องหลัก (Windows)
|
|-- backend/
|   |-- Dockerfile              # build backend image
|   |-- requirements.txt        # python dependencies
|   |-- api.py                  # Flask API (เรียก init_db ตอน start)
|   |-- database.py             # helper เปิด SQLite connection
|   |-- init_db.py              # สร้างตาราง DB (students, faces, attendance)
|   |-- data/
|   |   |-- attendance.db       # SQLite database (ถูกสร้างอัตโนมัติ)
|   |   |-- faces/              # (ยังว่าง) เก็บรูปหน้าที่ enroll
|
|-- frontend/
|   |-- index.html              # หน้าเว็บทดสอบ check-in / ดู attendance
|
|-- nginx/
|   |-- nginx.conf              # reverse proxy / serve frontend
```

## การนำไปใช้งานบน Raspberry Pi (Deployment)

### 1. สร้าง Backend Image (ทำบนคอมพิวเตอร์)
รันคำสั่งนี้บนคอมพิวเตอร์เพื่อสร้างและอัปโหลด Image ขึ้น Docker Hub (รองรับ Raspberry Pi):
```bash
docker buildx build --platform linux/amd64,linux/arm64,linux/arm/v7 -f backend/Dockerfile -t aphikap/face-attendance-backend:latest --push backend
```

### 2. เตรียมไฟล์บน Raspberry Pi
ก๊อปปี้ไฟล์และโฟลเดอร์เหล่านี้ไปวางบน Raspberry Pi (เช่นวางไว้ใน `~/face-attendance/`):

```
face-attendance/
├── docker-compose.yml          # ไฟล์ตั้งค่าสำคัญ
├── .env                        # ค่าตัวแปรระบบ (อย่าลืมแก้ PHONE_CAM_URL ในนี้)
├── frontend/                   # โฟลเดอร์หน้าเว็บ
│   └── index.html              # ไฟล์ html หลัก
└── nginx/                      # โฟลเดอร์ตั้งค่า Nginx
    └── nginx.conf              # ไฟล์ config
```

### 3. รันบน Raspberry Pi
 เข้าไปที่โฟลเดอร์นั้นใน Pi แล้วรันคำสั่ง:
```bash
# แก้ไขไฟล์ .env ให้ตรงกับ IP มือถือปัจจุบัน
nano .env

# เริ่มระบบ
docker-compose up -d
```
ระบบจะทำการโหลด (Pull) ตัว Backend Image มาให้อัตโนมัติและเริ่มทำงาน
เข้าใช้งานผ่าน: `http://<IP_ของ_RASPBERRY_PI>:8888` 
จัดโครงสร้างไฟล์ก่อน: เอากล่อง (Folder/Files) ไปวางเรียงให้ถูกที่ก่อน (เหมือนเราเตรียมเฟอร์นิเจอร์เข้าบ้านครับ)
เข้า cd face-attendance (หรือโฟลเดอร์ที่คุณสร้างไว้)
รันคำสั่งเดียวจบ:
bash
docker-compose up -d
👉 คำสั่งนี้มันฉลาดครับ
มันจะอ่านไฟล์ 
docker-compose.yml
มันเห็นว่าต้องใช้ Image ชื่อ aphikap/face-attendance-backend:latest
มันจะ Pull Image นั้นมาจาก Docker Hub ให้เองครับ (ถ้ายังไม่มีในเครื่อง)
พอดึงเสร็จ มันก็จะ Run ระบบให้ทันที
สรุป: คุณไม่ต้องพิมพ์คำสั่ง docker pull เองครับ เดี๋ยว docker-compose จัดการให้หมดครับ! แค่วางไฟล์ให้ถูกที่พอครับ

Goo